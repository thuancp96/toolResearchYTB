# Core (required)
PySide6>=6.6
opencv-python>=4.8
Pillow>=10.0
imageio-ffmpeg>=0.5      # provides the ffmpeg binary (downloaded on first use)

# Optional: speech-to-text auto description. The app runs without it
# (the Whisper feature is simply disabled until installed).
faster-whisper>=1.0

# Optional: download channel videos from the YouTube Finder tab.
# Without it the app still runs; the "Tải video" action just warns.
yt-dlp>=2024.1

# Optional: text-to-speech + slideshow video in the "Tạo ảnh AI" tab.
# Without it the app runs; the Voice/Video section is simply disabled.
edge-tts>=6.1
